import { StoryRatingstotalPipe } from './story-ratingstotal.pipe';

describe('StoryRatingstotalPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryRatingstotalPipe();
    expect(pipe).toBeTruthy();
  });
});
