import { ChapterIndexPipe } from './chapter-index.pipe';

describe('ChapterIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new ChapterIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
