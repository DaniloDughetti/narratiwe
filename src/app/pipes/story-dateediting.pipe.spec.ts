import { StoryDateeditingPipe } from './story-dateediting.pipe';

describe('StoryDateeditingPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryDateeditingPipe();
    expect(pipe).toBeTruthy();
  });
});
