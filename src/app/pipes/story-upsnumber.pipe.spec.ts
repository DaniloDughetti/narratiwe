import { StoryUpsnumberPipe } from './story-upsnumber.pipe';

describe('StoryUpsnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryUpsnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
