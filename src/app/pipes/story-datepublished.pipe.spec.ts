import { StoryDatepublishedPipe } from './story-datepublished.pipe';

describe('StoryDatepublishedPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryDatepublishedPipe();
    expect(pipe).toBeTruthy();
  });
});
