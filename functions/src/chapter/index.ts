import * as functions from 'firebase-functions';
import * as admin from 'firebase-admin';
/*
export const blockChapter = functions.database
    .ref('/chapters/{key}')
    .onUpdate(
        async event => {
            console.log("PROVA FUNCTIONS");
            console.log(event);
        }
    );

 export let block = functions.https.onRequest((request, response) => {
        console.log("Hello from Firebase!\n\n");
        var storyId = request.params.id;
        var lastIndex = request.params.lastIndex;
        console.log("** " + admin.database.ServerValue.TIMESTAMP);
        const now= Date.now();
        console.log('now=', now);
        console.log(now);
       });*/